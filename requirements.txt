# FINM 25000 — Homework 2
# Core data / numerics
pandas>=2.0
numpy>=1.24
scipy>=1.10

# Market data
alpaca-py>=0.30

# Charts & PDF report (PDF built with matplotlib's PdfPages — no extra deps)
matplotlib>=3.7

# Optional: fast parquet cache + Markdown table rendering in the report
pyarrow>=12.0
tabulate>=0.9
